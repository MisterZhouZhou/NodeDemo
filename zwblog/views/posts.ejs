<% posts.forEach(function(post,index){
if(index % 3==0){ %>
<div class="row">
    <%}%>
    <div class="span4">
        <h2><a href="/user/<%=post.username%>"><%=post.username %></a>说</h2>
        <p><small><%=post.time%></small></p>
        <p><%=post.post%></p>
    </div>
    <% if(index % 3 == 2){%>
</div> <!--结束行-->
<%}%>
<%})%>
<% if (posts.length % 3 !=0){ %>
</div>  <!--结束行-->
<%}%>